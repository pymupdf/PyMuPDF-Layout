

2. The import mappings window opens. Select the source host from which you want to import the volume mappings. As shown in Figure 8-39, we select the host **ITSO-VMHOST-01** and click **Import**.

![Screenshot of the 'Import Mappings' dialog box. The dialog shows 'Select Source Host:' with a dropdown menu displaying 'ITSO-VMHOST-01'. Below the dropdown are 'Cancel' and 'Import' buttons.](935eed7aa61f7777f62cfc032e11bee9_img.jpg)

Screenshot of the 'Import Mappings' dialog box. The dialog shows 'Select Source Host:' with a dropdown menu displaying 'ITSO-VMHOST-01'. Below the dropdown are 'Cancel' and 'Import' buttons.

Figure 8-39 import volume mappings source host selection

3. After the task completes, verify that the mappings are as expected from the **Hosts** menu (see Figure 8-26 on page 346), right-click the target host, and select **Properties**. Then, click the **Mapped Volumes** tab and verify that the required volumes were mapped to the new host (see Figure 8-37 on page 353).

# Renaming a host

To rename a host, complete the following steps:

1. Select the host, and then right-click and select **Rename** (see Figure 8-40).

![Screenshot showing the context menu for a host (ITSO-VMHOST-01) in the Hosts view. The 'Actions' menu is open, and the 'Rename...' option is highlighted. The table below shows the host details: Host Type: Generic, # of Ports: 2, Host Mappings: No.](4f4b52340aaccb1bcf733468dca9ee03_img.jpg)

Screenshot showing the context menu for a host (ITSO-VMHOST-01) in the Hosts view. The 'Actions' menu is open, and the 'Rename...' option is highlighted. The table below shows the host details: Host Type: Generic, # of Ports: 2, Host Mappings: No.

Figure 8-40 Rename a host